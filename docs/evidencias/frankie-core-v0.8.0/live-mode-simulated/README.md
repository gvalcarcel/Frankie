# Ejemplos simulados de Live Mode

Estos archivos proceden del simulador OFFLINE de Frankie Core. No son capturas de Frankie, no describen sus VMs y no constituyen evidencia LIVE.

- `live_status_simulated.json`: contrato de `live-status --simulate --json`.
- `live_audit_simulated.json`: contrato de `live-audit --simulate --json`.

Ambos conservan `enabled=false`, `connected=false`, `server_contacted=false`, `simulated=true` y `publishable=false`.
